describe('1111', () => {
  it('测试加法', () => {
    expect(1 + 1).toBe(2);
    expect(1 + 1).toBe(2);
  });

  it('测试 truthy', () => {
    expect(1).toBeTruthy();
    expect(0).toBeFalsy();
  });
});


